# Base URL of the Flask API. Leave empty in dev to use the Vite proxy
# (/api and /static are proxied to http://localhost:5000).
# In production point it at the deployed backend, e.g. https://api.example.com
VITE_API_BASE=
